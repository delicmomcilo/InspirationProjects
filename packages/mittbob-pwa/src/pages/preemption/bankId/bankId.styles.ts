import styled from 'styled-components';
import { ProgressIndicator as P } from '@bob/core-components';

export const ProgressIndicator = styled(P)`
  left: calc(50% - 2rem);
  top: calc(50% - 2rem);
  position: absolute;
`;
